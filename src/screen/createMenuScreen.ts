import { Screen, SpriteBatch } from "gdxjs";

const createMenuScreen = (batch: SpriteBatch): Screen<void> => {
  return {
    update(delta) {},
    dispose() {},
    init() {},
  };
};

export default createMenuScreen;
